# Optional production hardening.
# Pin exact SHA-256 values for the dependency versions you approve.
# The build scripts intentionally accept version inputs so CI can be used for
# upgrades without changing source files.
OPENSSL_SHA256=
NGHTTP2_SHA256=
CURL_SHA256=
